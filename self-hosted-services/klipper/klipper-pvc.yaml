apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: klipper-pvc
  namespace: klipper
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: crobasaurusrex-nfs
