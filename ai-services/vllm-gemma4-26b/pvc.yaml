apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-large-nfs-pvc
  namespace: vllm
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: crobasaurusrex-nfs
  resources:
    requests:
      storage: 256Gi
