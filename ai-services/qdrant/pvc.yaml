apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qdrant-nfs-pvc
  namespace: qdrant
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: crobasaurusrex-nfs
