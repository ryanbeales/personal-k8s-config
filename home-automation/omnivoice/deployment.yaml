apiVersion: apps/v1
kind: Deployment
metadata:
  name: omnivoice
  namespace: omnivoice
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: omnivoice
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: omnivoice
    spec:
      # Switch to the nvidia runtime class to get access to the GPU
      runtimeClassName: nvidia

      # Schedule omnivoice on GTX 1060
      nodeSelector:
        nvidia.com/gpu.product: NVIDIA-GeForce-GTX-1060-6GB

      containers:
      - name: omnivoice
        image: ghcr.io/ryanbeales/omnivoice-fastapi-pascal:latest
        imagePullPolicy: Always
        env:
        - name: TZ
          value: America/Vancouver
        resources:
          limits:
            nvidia.com/gpu: '1'
        ports:
        - name: http
          containerPort: 8880
        volumeMounts:
        - name: storage
          mountPath: /app/models
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: omnivoice-nfs-pvc
